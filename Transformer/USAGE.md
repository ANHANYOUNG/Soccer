# 5-Fold Training Guide

## 🚀 실행 방법

### 2. 학습 시작
```bash
./train_all_folds.sh
```

**동작 방식:**
- GPU 0: Fold 0 (Seed 42→123→456)
- GPU 1: Fold 1 (Seed 42→123→456)
- GPU 2: Fold 2 (Seed 42→123→456)
- GPU 3: Fold 3 (Seed 42→123→456)
- GPU 4: Fold 4 (Seed 42→123→456)

**각 fold는:**
1. Seed 42로 150 epoch 학습 → 모델 저장
2. Seed 123으로 150 epoch 학습 → 모델 저장
3. Seed 456으로 150 epoch 학습 → 모델 저장

**특징:**
- ✅ GPU당 1개 프로세스만 실행
- ✅ tqdm 진행률 바 활성화
- ✅ Ctrl+C로 모든 프로세스 깔끔하게 종료
- ✅ 백그라운드 실행으로 터미널 사용 가능

### 3. 진행 상황 확인

**방법: GPU 사용량**
```bash
watch -n 1 nvidia-smi
```

## 🎯 완료 후

```bash
python inference.py
```

## 📁 결과 구조

```
models/
├── label_encoders.pkl
├── seed_42_fold_0/
│   ├── best_model.pt
│   └── model_config.pkl
├── seed_42_fold_1/
├── seed_42_fold_2/
├── seed_42_fold_3/
├── seed_42_fold_4/
├── seed_123_fold_0/
├── ...
└── seed_456_fold_4/
```

총 15개 모델 (3 seeds × 5 folds)

